# Copy to .env.mac and adjust for your Mac Studio
# cp .env.mac.example .env.mac

OCR_LANG=en
OCR_DEFAULT_ENGINE=easyocr
OCR_ENABLED_ENGINES=easyocr
OCR_USE_GPU=1
PDF_RENDER_DPI=200

# Native deploy ports — change if 8000/3000 are taken
BACKEND_PORT=8100
FRONTEND_PORT=3100
